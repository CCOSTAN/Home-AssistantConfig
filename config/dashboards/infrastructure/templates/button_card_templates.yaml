######################################################################
# @CCOSTAN - Follow Me on X
# For more info visit https://www.vcloudinfo.com/click-here
# Original Repo : https://github.com/CCOSTAN/Home-AssistantConfig
# -------------------------------------------------------------------
# Infrastructure Templates - custom:button-card
# Related Issue: 1560
#  Shared `button_card_templates` used across Infrastructure and Overview views.
# -------------------------------------------------------------------
# Notes: Keep templates generic; view-specific tuning stays in the view partials.
# Notes: The compact ribbon template supports Overview status and daily controls.
# Notes: Mobile templates extend the ribbon with larger accessible tap targets and text.
# Notes: Named cards keep the icon beside the name; icons never consume a decorative row.
######################################################################

bearstone_infra_base:
  show_icon: true
  show_name: true
  show_state: true
  tap_action:
    action: more-info
  styles:
    card:
    - border-radius: 18px
    - padding: 14px
    - box-shadow: none
    - background: var(--ha-card-background, var(--card-background-color))
    - border: 1px solid var(--divider-color)
    - overflow: hidden
    grid:
    - grid-template-areas: "\"i n\" \"i s\""
    - grid-template-columns: 40px 1fr
    - grid-template-rows: min-content min-content
    icon:
    - width: 22px
    - color: var(--primary-text-color)
    name:
    - font-weight: 700
    - font-size: 14px
    - justify-self: start
    - padding-bottom: 2px
    state:
    - font-size: 12px
    - opacity: 0.75
    - justify-self: start

bearstone_infra_chip:
  template: bearstone_infra_base
  show_state: true
  styles:
    card:
    - border-radius: 999px
    - padding: 10px 12px
    - border: 1px solid var(--divider-color)
    - background: var(--ha-card-background, var(--card-background-color))
    grid:
    - grid-template-areas: "\"i n\" \"i s\""
    - grid-template-columns: 28px 1fr
    icon:
    - width: 18px
    name:
    - font-weight: 700
    - font-size: 12px
    state:
    - font-size: 11px
    - opacity: 0.7

bearstone_infra_chip_running:
  template: bearstone_infra_chip
  state:
  - value: 'on'
    styles:
      card:
      - border-color: rgba(67,160,71,0.45)
      - background: rgba(232,245,233,0.85)
      icon:
      - color: rgba(46,125,50,1)
  - value: 'off'
    styles:
      card:
      - border-color: rgba(229,57,53,0.35)
      - background: rgba(255,235,238,0.85)
      icon:
      - color: rgba(198,40,40,1)
  - value: unavailable
    styles:
      card:
      - border-color: rgba(229,57,53,0.35)
      - background: rgba(255,235,238,0.85)
      icon:
      - color: rgba(198,40,40,1)

bearstone_infra_tile:
  template: bearstone_infra_base
  show_label: true
  label: '[[[ return variables.label ? variables.label : "" ]]]'
  styles:
    grid:
    - grid-template-areas: "\"i n\" \"i l\""
    - grid-template-rows: min-content min-content
    label:
    - font-size: 12px
    - opacity: 0.8
    - justify-self: start
  state:
  - value: unavailable
    styles:
      card:
      - border-color: rgba(229,57,53,0.35)
      - background: rgba(255,235,238,0.85)

bearstone_infra_reboot:
  template: bearstone_infra_tile
  show_state: false
  icon: mdi:restart
  label: '[[[ return variables.subtitle ? variables.subtitle : "Hold to reboot" ]]]'
  hold_action:
    action: call-service
    service: button.press
    service_data:
      entity_id: '[[[ return variables.button_entity ]]]'
    confirmation:
      text: '[[[ return "Reboot " + (variables.name ? variables.name : "device") + "?" ]]]'
  styles:
    icon:
    - color: var(--secondary-text-color)

bearstone_infra_device_tile:
  template: bearstone_infra_tile
  show_state: true
  label: '[[[ return variables.subtitle ? variables.subtitle : "" ]]]'
  hold_action:
    action: call-service
    service: button.press
    service_data:
      entity_id: '[[[ return variables.button_entity ]]]'
    confirmation:
      text: '[[[ return "Restart " + (variables.name ? variables.name : "device") + "?" ]]]'
  styles:
    state:
    - font-weight: 700

bearstone_infra_prune_tile:
  template: bearstone_infra_tile
  show_state: true
  icon: mdi:image-sync
  label: >-
    [[[
      return variables.subtitle ? variables.subtitle : "Hold to prune unused images";
    ]]]
  hold_action:
    action: call-service
    service: button.press
    service_data:
      entity_id: '[[[ return variables.prune_button ]]]'
    confirmation:
      text: '[[[ return "Prune unused images on " + (variables.name ? variables.name : "host") + "?" ]]]'
  styles:
    icon:
    - color: var(--secondary-text-color)
    state:
    - font-weight: 700

bearstone_infra_volume_prune_tile:
  template: bearstone_infra_tile
  show_state: true
  icon: mdi:database-remove
  label: >-
    [[[
      return variables.subtitle ? variables.subtitle : "Hold to prune unused volumes";
    ]]]
  hold_action:
    action: call-service
    service: button.press
    service_data:
      entity_id: '[[[ return variables.prune_button ]]]'
    confirmation:
      text: '[[[ return "Prune unused volumes on " + (variables.name ? variables.name : "host") + "?" ]]]'
  styles:
    icon:
    - color: var(--secondary-text-color)
    state:
    - font-weight: 700

bearstone_infra_apt_prune_tile:
  template: bearstone_infra_prune_tile
  show_state: true
  icon: mdi:server
  label: >-
    [[[
      const lastSensor = variables.last_update_sensor ? variables.last_update_sensor : '';
      const lastValue = lastSensor ? (states[lastSensor]?.state ?? 'unknown') : 'unknown';
      return "Last update: " + lastValue + " | Hold to prune";
    ]]]

bearstone_infra_container_row:
  template: bearstone_infra_list_row
  show_label: false
  show_state: false
  tap_action:
    action: none
  hold_action:
    action: call-service
    service: button.press
    service_data:
      entity_id: >
        [[[
          if (variables && variables.restart_button) return variables.restart_button;
          const ent = (entity && entity.entity_id) ? String(entity.entity_id) : '';
          let key = '';
          if (ent.startsWith('binary_sensor.') && ent.endsWith('_status')) {
            key = ent.replace('binary_sensor.', '').replace(/_status$/, '');
          } else if (ent.startsWith('binary_sensor.') && ent.endsWith('_status_2')) {
            key = ent.replace('binary_sensor.', '').replace(/_status_2$/, '');
          } else if (ent.startsWith('switch.') && ent.endsWith('_container')) {
            key = ent.replace('switch.', '').replace(/_container$/, '');
          } else if (ent.startsWith('switch.') && ent.endsWith('_container_2')) {
            key = ent.replace('switch.', '').replace(/_container_2$/, '');
          }
          if (!key) return '';
          const restartCandidates = [
            `button.${key}_restart_container`,
            `button.${key}_restart_container_2`,
          ];
          for (const candidate of restartCandidates) {
            if (states[candidate]) return candidate;
          }
          return restartCandidates[0];
        ]]]
    confirmation:
      text: >
        [[[
          const friendly =
            (entity && entity.attributes && entity.attributes.friendly_name)
              ? String(entity.attributes.friendly_name)
              : ((entity && entity.entity_id) ? String(entity.entity_id) : 'container');
          return "Restart container " + friendly + "?";
        ]]]
  icon: mdi:docker
  name: >
    [[[
      if (variables && variables.name) return variables.name;
      const ent = (entity && entity.entity_id) ? String(entity.entity_id) : '';
      const friendly = (entity && entity.attributes && entity.attributes.friendly_name)
        ? String(entity.attributes.friendly_name)
        : '';
      let key = '';
      if (ent.startsWith('binary_sensor.') && ent.endsWith('_status')) {
        key = ent.replace('binary_sensor.', '').replace(/_status$/, '');
      } else if (ent.startsWith('binary_sensor.') && ent.endsWith('_status_2')) {
        key = ent.replace('binary_sensor.', '').replace(/_status_2$/, '');
      } else if (ent.startsWith('switch.') && ent.endsWith('_container')) {
        key = ent.replace('switch.', '').replace(/_container$/, '');
      } else if (ent.startsWith('switch.') && ent.endsWith('_container_2')) {
        key = ent.replace('switch.', '').replace(/_container_2$/, '');
      }
      if (friendly && friendly !== 'Container') {
        return friendly.replace(/\s+Container$/, '');
      }
      return key || friendly || ent;
    ]]]
  custom_fields:
    image: >
      [[[
        const ent = (entity && entity.entity_id) ? String(entity.entity_id) : '';
        const telemetryDegraded = states['binary_sensor.docker_container_telemetry_degraded']?.state === 'on';
        let key = '';
        if (ent.startsWith('binary_sensor.') && ent.endsWith('_status')) {
          key = ent.replace('binary_sensor.', '').replace(/_status$/, '');
        } else if (ent.startsWith('binary_sensor.') && ent.endsWith('_status_2')) {
          key = ent.replace('binary_sensor.', '').replace(/_status_2$/, '');
        } else if (ent.startsWith('switch.') && ent.endsWith('_container')) {
          key = ent.replace('switch.', '').replace(/_container$/, '');
        } else if (ent.startsWith('switch.') && ent.endsWith('_container_2')) {
          key = ent.replace('switch.', '').replace(/_container_2$/, '');
        }
        const stateCandidates = key ? [
          `binary_sensor.${key}_status`,
          `binary_sensor.${key}_status_2`,
          `sensor.${key}_state`,
          `sensor.${key}_state_2`,
          `switch.${key}_container`,
          `switch.${key}_container_2`,
        ] : [];
        const isUnknownLike = (v) => !v || ['unknown', 'unavailable', 'none', ''].includes(String(v).toLowerCase());
        let resolvedState = String(entity && entity.state !== undefined ? entity.state : '').toLowerCase();
        for (const candidate of stateCandidates) {
          const candidateState = states[candidate]?.state;
          if (!isUnknownLike(candidateState)) {
            resolvedState = String(candidateState).toLowerCase();
            break;
          }
        }
        if (isUnknownLike(resolvedState)) {
          for (const candidate of stateCandidates) {
            const candidateState = states[candidate]?.state;
            if (candidateState !== undefined) {
              resolvedState = String(candidateState).toLowerCase();
              break;
            }
          }
        }
        const imageCandidates = variables.image_sensor
          ? [variables.image_sensor]
          : (key ? [`sensor.${key}_image`, `sensor.${key}_image_2`] : []);
        let imageValue;
        for (const candidate of imageCandidates) {
          const candidateState = states[candidate]?.state;
          if (!isUnknownLike(candidateState)) {
            imageValue = candidateState;
            break;
          }
        }
        if (imageValue === undefined) {
          for (const candidate of imageCandidates) {
            const candidateState = states[candidate]?.state;
            if (candidateState !== undefined) {
              imageValue = candidateState;
              break;
            }
          }
        }
        if (isUnknownLike(imageValue)) {
          if (telemetryDegraded && ['unknown', 'unavailable', ''].includes(resolvedState)) {
            return 'telemetry: delayed';
          }
          return 'image: n/a';
        }
        return imageValue;
      ]]]
    status: >
      [[[
        const ent = (entity && entity.entity_id) ? String(entity.entity_id) : '';
        let key = '';
        if (ent.startsWith('binary_sensor.') && ent.endsWith('_status')) {
          key = ent.replace('binary_sensor.', '').replace(/_status$/, '');
        } else if (ent.startsWith('binary_sensor.') && ent.endsWith('_status_2')) {
          key = ent.replace('binary_sensor.', '').replace(/_status_2$/, '');
        } else if (ent.startsWith('switch.') && ent.endsWith('_container')) {
          key = ent.replace('switch.', '').replace(/_container$/, '');
        } else if (ent.startsWith('switch.') && ent.endsWith('_container_2')) {
          key = ent.replace('switch.', '').replace(/_container_2$/, '');
        }
        const candidates = key ? [
          `binary_sensor.${key}_status`,
          `binary_sensor.${key}_status_2`,
          `sensor.${key}_state`,
          `sensor.${key}_state_2`,
          `switch.${key}_container`,
          `switch.${key}_container_2`,
        ] : [];
        const isUnknownLike = (v) => !v || ['unknown', 'unavailable', ''].includes(String(v).toLowerCase());
        let s = String(entity && entity.state !== undefined ? entity.state : '').toLowerCase();
        for (const candidate of candidates) {
          const candidateState = states[candidate]?.state;
          if (!isUnknownLike(candidateState)) {
            s = String(candidateState).toLowerCase();
            break;
          }
        }
        if (isUnknownLike(s)) {
          for (const candidate of candidates) {
            const candidateState = states[candidate]?.state;
            if (candidateState !== undefined) {
              s = String(candidateState).toLowerCase();
              break;
            }
          }
        }
        const telemetryDegraded = states['binary_sensor.docker_container_telemetry_degraded']?.state === 'on';
        if (s === 'on' || s === 'running') return 'RUNNING';
        if (s === 'off' || s === 'stopped' || s === 'exited' || s === 'dead') return 'STOPPED';
        if (s === 'unavailable') return telemetryDegraded ? 'STALE' : 'OFFLINE';
        if (s === 'unknown' || s === '') return telemetryDegraded ? 'STALE' : 'UNKNOWN';
        return String(s).toUpperCase();
      ]]]
  styles:
    grid:
    - grid-template-areas: "\"i n status\" \"i image status\""
    - grid-template-columns: 24px 1fr auto
    - grid-template-rows: min-content min-content
    - align-items: center
    - column-gap: 12px
    name:
    - min-width: 0
    - overflow: hidden
    - text-overflow: ellipsis
    - white-space: nowrap
    icon:
    - color: >
        [[[
          const ent = (entity && entity.entity_id) ? String(entity.entity_id) : '';
          let key = '';
          if (ent.startsWith('binary_sensor.') && ent.endsWith('_status')) {
            key = ent.replace('binary_sensor.', '').replace(/_status$/, '');
          } else if (ent.startsWith('binary_sensor.') && ent.endsWith('_status_2')) {
            key = ent.replace('binary_sensor.', '').replace(/_status_2$/, '');
          } else if (ent.startsWith('switch.') && ent.endsWith('_container')) {
            key = ent.replace('switch.', '').replace(/_container$/, '');
          } else if (ent.startsWith('switch.') && ent.endsWith('_container_2')) {
            key = ent.replace('switch.', '').replace(/_container_2$/, '');
          }
          const candidates = key ? [
            `binary_sensor.${key}_status`,
            `binary_sensor.${key}_status_2`,
            `sensor.${key}_state`,
            `sensor.${key}_state_2`,
            `switch.${key}_container`,
            `switch.${key}_container_2`,
          ] : [];
          const isUnknownLike = (v) => !v || ['unknown', 'unavailable', ''].includes(String(v).toLowerCase());
          let s = String(entity && entity.state !== undefined ? entity.state : '').toLowerCase();
          for (const candidate of candidates) {
            const candidateState = states[candidate]?.state;
            if (!isUnknownLike(candidateState)) {
              s = String(candidateState).toLowerCase();
              break;
            }
          }
          if (isUnknownLike(s)) {
            for (const candidate of candidates) {
              const candidateState = states[candidate]?.state;
              if (candidateState !== undefined) {
                s = String(candidateState).toLowerCase();
                break;
              }
            }
          }
          if (s === 'on' || s === 'running') return 'rgba(46,125,50,1)';
          if (s === 'off' || s === 'stopped' || s === 'exited' || s === 'dead') return 'rgba(198,40,40,1)';
          return 'rgba(230,81,0,1)';
        ]]]
    custom_fields:
      image:
      - grid-area: image
      - justify-self: start
      - align-self: start
      - font-size: 11px
      - opacity: 0.72
      - line-height: 1.1
      - white-space: nowrap
      - overflow: hidden
      - text-overflow: ellipsis
      - max-width: 100%
      status:
      - justify-self: end
      - align-self: center
      - font-weight: 900
      - font-size: 12px
      - letter-spacing: 0.04em
      - padding: 4px 10px
      - border-radius: 999px
      - background: >
          [[[
            const ent = (entity && entity.entity_id) ? String(entity.entity_id) : '';
            let key = '';
            if (ent.startsWith('binary_sensor.') && ent.endsWith('_status')) {
              key = ent.replace('binary_sensor.', '').replace(/_status$/, '');
            } else if (ent.startsWith('binary_sensor.') && ent.endsWith('_status_2')) {
              key = ent.replace('binary_sensor.', '').replace(/_status_2$/, '');
            } else if (ent.startsWith('switch.') && ent.endsWith('_container')) {
              key = ent.replace('switch.', '').replace(/_container$/, '');
            } else if (ent.startsWith('switch.') && ent.endsWith('_container_2')) {
              key = ent.replace('switch.', '').replace(/_container_2$/, '');
            }
            const candidates = key ? [
              `binary_sensor.${key}_status`,
              `binary_sensor.${key}_status_2`,
              `sensor.${key}_state`,
              `sensor.${key}_state_2`,
              `switch.${key}_container`,
              `switch.${key}_container_2`,
            ] : [];
            const isUnknownLike = (v) => !v || ['unknown', 'unavailable', ''].includes(String(v).toLowerCase());
            let s = String(entity && entity.state !== undefined ? entity.state : '').toLowerCase();
            for (const candidate of candidates) {
              const candidateState = states[candidate]?.state;
              if (!isUnknownLike(candidateState)) {
                s = String(candidateState).toLowerCase();
                break;
              }
            }
            if (isUnknownLike(s)) {
              for (const candidate of candidates) {
                const candidateState = states[candidate]?.state;
                if (candidateState !== undefined) {
                  s = String(candidateState).toLowerCase();
                  break;
                }
              }
            }
            if (s === 'on' || s === 'running') return 'rgba(46,125,50,0.12)';
            if (s === 'off' || s === 'stopped' || s === 'exited' || s === 'dead') return 'rgba(198,40,40,0.10)';
            return 'rgba(230,81,0,0.12)';
          ]]]
      - color: >
          [[[
            const ent = (entity && entity.entity_id) ? String(entity.entity_id) : '';
            let key = '';
            if (ent.startsWith('binary_sensor.') && ent.endsWith('_status')) {
              key = ent.replace('binary_sensor.', '').replace(/_status$/, '');
            } else if (ent.startsWith('binary_sensor.') && ent.endsWith('_status_2')) {
              key = ent.replace('binary_sensor.', '').replace(/_status_2$/, '');
            } else if (ent.startsWith('switch.') && ent.endsWith('_container')) {
              key = ent.replace('switch.', '').replace(/_container$/, '');
            } else if (ent.startsWith('switch.') && ent.endsWith('_container_2')) {
              key = ent.replace('switch.', '').replace(/_container_2$/, '');
            }
            const candidates = key ? [
              `binary_sensor.${key}_status`,
              `binary_sensor.${key}_status_2`,
              `sensor.${key}_state`,
              `sensor.${key}_state_2`,
              `switch.${key}_container`,
              `switch.${key}_container_2`,
            ] : [];
            const isUnknownLike = (v) => !v || ['unknown', 'unavailable', ''].includes(String(v).toLowerCase());
            let s = String(entity && entity.state !== undefined ? entity.state : '').toLowerCase();
            for (const candidate of candidates) {
              const candidateState = states[candidate]?.state;
              if (!isUnknownLike(candidateState)) {
                s = String(candidateState).toLowerCase();
                break;
              }
            }
            if (isUnknownLike(s)) {
              for (const candidate of candidates) {
                const candidateState = states[candidate]?.state;
                if (candidateState !== undefined) {
                  s = String(candidateState).toLowerCase();
                  break;
                }
              }
            }
            if (s === 'on' || s === 'running') return 'rgba(46,125,50,1)';
            if (s === 'off' || s === 'stopped' || s === 'exited' || s === 'dead') return 'rgba(198,40,40,1)';
            return 'rgba(230,81,0,1)';
          ]]]
    card:
    - border-color: >
        [[[
          const ent = (entity && entity.entity_id) ? String(entity.entity_id) : '';
          let key = '';
          if (ent.startsWith('binary_sensor.') && ent.endsWith('_status')) {
            key = ent.replace('binary_sensor.', '').replace(/_status$/, '');
          } else if (ent.startsWith('binary_sensor.') && ent.endsWith('_status_2')) {
            key = ent.replace('binary_sensor.', '').replace(/_status_2$/, '');
          } else if (ent.startsWith('switch.') && ent.endsWith('_container')) {
            key = ent.replace('switch.', '').replace(/_container$/, '');
          } else if (ent.startsWith('switch.') && ent.endsWith('_container_2')) {
            key = ent.replace('switch.', '').replace(/_container_2$/, '');
          }
          const candidates = key ? [
            `binary_sensor.${key}_status`,
            `binary_sensor.${key}_status_2`,
            `sensor.${key}_state`,
            `sensor.${key}_state_2`,
            `switch.${key}_container`,
            `switch.${key}_container_2`,
          ] : [];
          const isUnknownLike = (v) => !v || ['unknown', 'unavailable', ''].includes(String(v).toLowerCase());
          let s = String(entity && entity.state !== undefined ? entity.state : '').toLowerCase();
          for (const candidate of candidates) {
            const candidateState = states[candidate]?.state;
            if (!isUnknownLike(candidateState)) {
              s = String(candidateState).toLowerCase();
              break;
            }
          }
          if (isUnknownLike(s)) {
            for (const candidate of candidates) {
              const candidateState = states[candidate]?.state;
              if (candidateState !== undefined) {
                s = String(candidateState).toLowerCase();
                break;
              }
            }
          }
          if (s === 'on' || s === 'running') return 'rgba(67,160,71,0.45)';
          if (s === 'off' || s === 'stopped' || s === 'exited' || s === 'dead') return 'rgba(229,57,53,0.35)';
          return 'rgba(245,124,0,0.35)';
        ]]]
    - background: >
        [[[
          const ent = (entity && entity.entity_id) ? String(entity.entity_id) : '';
          let key = '';
          if (ent.startsWith('binary_sensor.') && ent.endsWith('_status')) {
            key = ent.replace('binary_sensor.', '').replace(/_status$/, '');
          } else if (ent.startsWith('binary_sensor.') && ent.endsWith('_status_2')) {
            key = ent.replace('binary_sensor.', '').replace(/_status_2$/, '');
          } else if (ent.startsWith('switch.') && ent.endsWith('_container')) {
            key = ent.replace('switch.', '').replace(/_container$/, '');
          } else if (ent.startsWith('switch.') && ent.endsWith('_container_2')) {
            key = ent.replace('switch.', '').replace(/_container_2$/, '');
          }
          const candidates = key ? [
            `binary_sensor.${key}_status`,
            `binary_sensor.${key}_status_2`,
            `sensor.${key}_state`,
            `sensor.${key}_state_2`,
            `switch.${key}_container`,
            `switch.${key}_container_2`,
          ] : [];
          const isUnknownLike = (v) => !v || ['unknown', 'unavailable', ''].includes(String(v).toLowerCase());
          let s = String(entity && entity.state !== undefined ? entity.state : '').toLowerCase();
          for (const candidate of candidates) {
            const candidateState = states[candidate]?.state;
            if (!isUnknownLike(candidateState)) {
              s = String(candidateState).toLowerCase();
              break;
            }
          }
          if (isUnknownLike(s)) {
            for (const candidate of candidates) {
              const candidateState = states[candidate]?.state;
              if (candidateState !== undefined) {
                s = String(candidateState).toLowerCase();
                break;
              }
            }
          }
          if (s === 'on' || s === 'running') return 'rgba(232,245,233,0.85)';
          if (s === 'off' || s === 'stopped' || s === 'exited' || s === 'dead') return 'rgba(255,235,238,0.85)';
          return 'rgba(255,243,224,0.85)';
        ]]]
    - display: >
        [[[
          const ent = (entity && entity.entity_id) ? String(entity.entity_id) : '';
          let key = '';
          if (ent.startsWith('binary_sensor.') && ent.endsWith('_status')) {
            key = ent.replace('binary_sensor.', '').replace(/_status$/, '');
          } else if (ent.startsWith('binary_sensor.') && ent.endsWith('_status_2')) {
            key = ent.replace('binary_sensor.', '').replace(/_status_2$/, '');
          } else if (ent.startsWith('switch.') && ent.endsWith('_container')) {
            key = ent.replace('switch.', '').replace(/_container$/, '');
          } else if (ent.startsWith('switch.') && ent.endsWith('_container_2')) {
            key = ent.replace('switch.', '').replace(/_container_2$/, '');
          }
          const switchEntity = key ? `switch.${key}_container` : '';
          const switchEntityAlt = key ? `switch.${key}_container_2` : '';
          const monitored = states['sensor.docker_monitored_switch_inventory']?.attributes?.entity_id;
          const restartCandidates = key ? [
            `button.${key}_restart_container`,
            `button.${key}_restart_container_2`,
          ] : [];
          const hasRestart = restartCandidates.some((candidate) => states[candidate]);
          const isMonitored = Array.isArray(monitored)
            ? monitored.includes(switchEntity) || monitored.includes(switchEntityAlt)
            : (ent.startsWith('switch.') && (ent.endsWith('_container') || ent.endsWith('_container_2')));
          return (hasRestart && isMonitored) ? 'block' : 'none';
        ]]]

bearstone_infra_panel_header:
  show_icon: false
  show_state: false
  show_label: false
  tap_action:
    action: none
  styles:
    card:
    - background: transparent
    - border: none
    - box-shadow: none
    - padding: 6px 2px 10px 2px
    name:
    - font-size: 18px
    - font-weight: 800
    - letter-spacing: 0.04em
    - text-transform: uppercase
    - justify-self: start

bearstone_infra_panel_header_icon:
  template: bearstone_infra_panel_header
  show_icon: true
  show_name: true
  icon: mdi:view-dashboard-outline
  styles:
    card:
    - padding: 6px 2px 10px 2px
    grid:
    - grid-template-areas: '"i n"'
    - grid-template-columns: 20px minmax(0, auto)
    - column-gap: 6px
    - justify-content: start
    icon:
    - width: 18px
    - color: var(--primary-color)
    - justify-self: start
    name:
    - justify-self: start

bearstone_infra_list_row:
  show_icon: true
  show_name: true
  show_state: true
  show_label: false
  tap_action:
    action: more-info
  styles:
    card:
    - border-radius: 14px
    - padding: 12px 12px
    - box-shadow: none
    - border: 1px solid rgba(0,0,0,0.03)
    - background: rgba(0,0,0,0.02)
    - box-sizing: border-box
    grid:
    - grid-template-areas: "\"i n s\""
    - grid-template-columns: 24px 1fr auto
    - align-items: center
    - column-gap: 10px
    icon:
    - width: 18px
    - color: var(--primary-color)
    name:
    - font-weight: 700
    - font-size: 14px
    - justify-self: start
    - opacity: 0.95
    - min-width: 0
    - overflow: hidden
    - text-overflow: ellipsis
    - white-space: nowrap
    state:
    - justify-self: end
    - font-weight: 700
    - font-size: 13px
    - opacity: 0.8
    - padding-left: 8px
    - white-space: nowrap

bearstone_infra_alert_row:
  template: bearstone_infra_list_row
  # Home view only. Uses JS visibility checks to keep Home "exceptions-only".
  show_label: true
  label: >
    [[[ return String(variables?.exception_detail ?? ''); ]]]
  variables:
    alert_severity: >
      [[[
        const normalizeIds = (value) => {
          if (Array.isArray(value)) return value.map(String);
          if (typeof value === 'string' && value.trim()) return [value.trim()];
          return [];
        };
        const anyOn = (ids) => ids.some((id) => String(states[id]?.state ?? '').toLowerCase() === 'on');
        if (anyOn(normalizeIds(variables?.critical_entity_ids))) return 'critical';
        if (anyOn(normalizeIds(variables?.warning_entity_ids)) || variables?.warning_only) return 'warning';
        return 'critical';
      ]]]
  styles:
    grid:
    - grid-template-areas: '"i n s" "i l l"'
    - grid-template-columns: 24px 1fr auto
    - grid-template-rows: min-content min-content
    card:
    - border-color: >
        [[[
          return variables?.alert_severity === 'warning'
            ? 'rgba(245,124,0,0.35)'
            : 'rgba(229,57,53,0.25)';
        ]]]
    - background: >
        [[[
          return variables?.alert_severity === 'warning'
            ? 'rgba(255,243,224,0.75)'
            : 'rgba(255,235,238,0.65)';
        ]]]
    - display: >
        [[[
          const kind = (variables && variables.alert_kind) ? String(variables.alert_kind) : '';
          const minAge = (variables && variables.min_age_s !== undefined) ? Number(variables.min_age_s) : 0;
          const thr = (variables && variables.threshold !== undefined) ? Number(variables.threshold) : null;

          const stateStr = String(entity && entity.state !== undefined ? entity.state : '');
          const lastChanged = entity && entity.last_changed ? new Date(entity.last_changed) : null;
          // If we can't determine age, do not surface time-gated alerts.
          const ageS = lastChanged ? ((Date.now() - lastChanged.getTime()) / 1000) : 0;

          let show = false;
          if (kind === 'binary_on') show = (stateStr === 'on');
          else if (kind === 'binary_off') show = (stateStr === 'off');
          else if (kind === 'state_not') {
            const okState = String(variables?.ok_state ?? '').toLowerCase();
            show = stateStr.toLowerCase() !== okState;
          }
          else if (kind === 'ap_zero') show = (stateStr === '0' && ageS >= minAge);
          else if (kind === 'disk_high' || kind === 'numeric_high') {
            const v = parseFloat(stateStr);
            show = Number.isFinite(v) && (thr !== null ? (v > thr) : (v > 80));
          }

          return show ? 'block' : 'none';
        ]]]
    icon:
    - color: >
        [[[
          return variables?.alert_severity === 'warning'
            ? 'rgba(230,81,0,1)'
            : 'rgba(198,40,40,1)';
        ]]]
    state:
    - color: >
        [[[
          return variables?.alert_severity === 'warning'
            ? 'rgba(230,81,0,1)'
            : 'rgba(198,40,40,1)';
        ]]]
    label:
    - grid-area: l
    - justify-self: start
    - padding-top: 3px
    - font-size: 11px
    - font-weight: 700
    - line-height: 14px
    - opacity: 0.78
    - white-space: normal
    - text-align: left

bearstone_infra_list_row_running:
  template: bearstone_infra_list_row
  state:
  - value: 'on'
    styles:
      card:
      - border-color: rgba(67,160,71,0.45)
      - background: rgba(232,245,233,0.85)
      icon:
      - color: rgba(46,125,50,1)
      state:
      - color: rgba(46,125,50,1)
      - opacity: 1
  - value: 'off'
    styles:
      card:
      - border-color: rgba(229,57,53,0.35)
      - background: rgba(255,235,238,0.85)
      icon:
      - color: rgba(198,40,40,1)
      state:
      - color: rgba(198,40,40,1)
      - opacity: 1
  - value: Running
    styles:
      icon:
      - color: rgba(46,125,50,1)
      state:
      - color: rgba(46,125,50,1)
      - opacity: 1
  - value: running
    styles:
      icon:
      - color: rgba(46,125,50,1)
      state:
      - color: rgba(46,125,50,1)
      - opacity: 1
  - value: Stopped
    styles:
      icon:
      - color: rgba(198,40,40,1)
      state:
      - color: rgba(198,40,40,1)
      - opacity: 1
  - value: stopped
    styles:
      icon:
      - color: rgba(198,40,40,1)
      state:
      - color: rgba(198,40,40,1)
      - opacity: 1
  - value: unavailable
    styles:
      icon:
      - color: rgba(198,40,40,1)
      state:
      - color: rgba(198,40,40,1)
      - opacity: 1

bearstone_infra_kpi:
  show_icon: true
  show_name: true
  show_state: true
  show_label: false
  tap_action:
    action: more-info
  styles:
    card:
    - border-radius: 14px
    - padding: 12px
    - box-shadow: none
    - border: 1px solid rgba(0,0,0,0.03)
    - background: rgba(0,0,0,0.02)
    grid:
    - grid-template-areas: '"i n" "s s"'
    - grid-template-columns: 18px minmax(0, auto)
    - grid-template-rows: 20px min-content
    - column-gap: 6px
    - row-gap: 5px
    - justify-content: center
    icon:
    - width: 18px
    - color: var(--primary-color)
    - justify-self: end
    name:
    - font-size: 11px
    - font-weight: 800
    - opacity: 0.65
    - text-transform: uppercase
    - text-align: left
    - letter-spacing: 0.6px
    - justify-self: start
    state:
    - font-size: 16px
    - font-weight: 800
    - opacity: 0.9
    - justify-self: center

bearstone_infra_status_kpi:
  template: bearstone_infra_kpi
  variables:
    status_severity: >
      [[[
        const raw = String(entity?.state ?? '').toLowerCase();
        const unavailable = ['unknown', 'unavailable', 'none', ''].includes(raw);
        if (unavailable) return 'unknown';
        const normalizeIds = (value) => {
          if (Array.isArray(value)) return value.map(String);
          if (typeof value === 'string' && value.trim()) return [value.trim()];
          return [];
        };
        const anyOn = (ids) => ids.some((id) => String(states[id]?.state ?? '').toLowerCase() === 'on');
        const critical = anyOn(normalizeIds(variables?.critical_entity_ids));
        const warning = anyOn(normalizeIds(variables?.warning_entity_ids));
        const warningState = String(variables?.warning_state ?? '').toLowerCase();
        const okState = String(variables?.ok_state ?? 'off').toLowerCase();
        if (critical) return 'critical';
        if (warning || (warningState && raw === warningState)) return 'warning';
        return raw === okState ? 'healthy' : 'critical';
      ]]]
  state_display: >
    [[[
      const raw = String(entity?.state ?? '').toLowerCase();
      const severity = String(variables?.status_severity ?? '');
      if (severity === 'unknown') return 'NO DATA';
      if (variables?.pending_detail && raw === 'on') {
        const countEntity = String(variables?.pending_count_entity ?? '').trim();
        const countRaw = countEntity ? states[countEntity]?.state : '';
        const count = Number.parseInt(countRaw, 10);
        const countText = Number.isFinite(count) && count > 0
          ? `${count} PKG${count === 1 ? '' : 'S'}`
          : 'UPDATES';
        const next = new Date();
        next.setHours(Number(variables?.pending_schedule_hour ?? 2), Number(variables?.pending_schedule_minute ?? 15), 0, 0);
        if (next <= new Date()) next.setDate(next.getDate() + 1);
        const dateText = next.toLocaleDateString(undefined, {month: 'short', day: 'numeric'}).toUpperCase();
        const timeText = next.toLocaleTimeString(undefined, {hour: 'numeric', minute: '2-digit'});
        return `${countText} · NEXT CHECK ${dateText} ${timeText}`;
      }
      if (severity === 'warning') return variables?.warning_text ?? variables?.alert_text ?? 'DEGRADED';
      if (severity === 'healthy') return variables?.healthy_text ?? 'HEALTHY';
      if (variables?.critical_text) return variables.critical_text;
      if (variables?.alert_text) return variables.alert_text;
      if (variables?.raw_suffix) return `${entity.state} ${variables.raw_suffix}`;
      return 'ATTENTION';
    ]]]
  styles:
    card:
    - border-color: >
        [[[
          const severity = String(variables?.status_severity ?? '');
          if (severity === 'healthy') return 'rgba(67,160,71,0.45)';
          if (severity === 'critical') return 'rgba(229,57,53,0.40)';
          return 'rgba(245,124,0,0.45)';
        ]]]
    - background: >
        [[[
          const severity = String(variables?.status_severity ?? '');
          if (severity === 'healthy') return 'rgba(232,245,233,0.85)';
          if (severity === 'critical') return 'rgba(255,235,238,0.85)';
          return 'rgba(255,243,224,0.85)';
        ]]]
    icon:
    - color: >
        [[[
          const severity = String(variables?.status_severity ?? '');
          if (severity === 'healthy') return 'rgba(46,125,50,1)';
          if (severity === 'critical') return 'rgba(198,40,40,1)';
          return 'rgba(230,81,0,1)';
        ]]]
    state:
    - color: >
        [[[
          const severity = String(variables?.status_severity ?? '');
          if (severity === 'healthy') return 'rgba(46,125,50,1)';
          if (severity === 'critical') return 'rgba(198,40,40,1)';
          return 'rgba(230,81,0,1)';
        ]]]
    - opacity: 1

bearstone_overview_ribbon_control:
  template: bearstone_infra_chip
  variables:
    ok_states: []
    active_states: []
    warning_states: []
    warning_entity_ids: []
    critical_entity_ids: []
    state_labels: {}
    raw_suffix: ''
    display_mode: state
    humidity_entity: ''
    cooling_entity: ''
    cooling_active: >
      [[[
        const coolingEntity = String(variables?.cooling_entity ?? '').trim();
        return Boolean(coolingEntity && states[coolingEntity]?.state === 'on');
      ]]]
    mobile_compact: >
      [[[ return window.innerWidth <= 600; ]]]
    status_severity: >
      [[[
        const raw = String(entity?.state ?? '').toLowerCase();
        const normalizeIds = (value) => {
          if (Array.isArray(value)) return value.map(String);
          if (typeof value === 'string' && value.trim()) return [value.trim()];
          return [];
        };
        const anyOn = (ids) => ids.some((id) => String(states[id]?.state ?? '').toLowerCase() === 'on');
        const ok = Array.isArray(variables?.ok_states) ? variables.ok_states.map(String) : [];
        const active = Array.isArray(variables?.active_states) ? variables.active_states.map(String) : [];
        const warning = Array.isArray(variables?.warning_states) ? variables.warning_states.map(String) : [];
        if (anyOn(normalizeIds(variables?.critical_entity_ids))) return 'critical';
        if (anyOn(normalizeIds(variables?.warning_entity_ids))) return 'warning';
        if (variables?.cooling_active || active.includes(raw)) return 'active';
        if (['unknown', 'unavailable', 'none', ''].includes(raw) || warning.includes(raw)) return 'warning';
        return ok.includes(raw) ? 'healthy' : 'critical';
      ]]]
  state_display: >
    [[[
      const raw = String(entity?.state ?? 'unknown').toLowerCase();
      if (variables?.display_mode === 'climate') {
        const current = Number(entity?.attributes?.current_temperature);
        const target = Number(entity?.attributes?.temperature);
        const humidityEntity = String(variables?.humidity_entity ?? '').trim();
        const humidity = humidityEntity ? Number(states[humidityEntity]?.state) : Number.NaN;
        const currentText = Number.isFinite(current) ? `${Math.round(current)}°` : 'n/a';
        const targetText = Number.isFinite(target) ? `${Math.round(target)}°` : 'n/a';
        const humidityText = Number.isFinite(humidity) ? ` · ${Math.round(humidity)}%` : '';
        if (variables?.mobile_compact) return `${currentText}/${targetText}${humidityText}`;
        return `${currentText} / ${targetText}${humidityText}`;
      }
      const labels = variables?.state_labels || {};
      if (Object.prototype.hasOwnProperty.call(labels, raw)) return labels[raw];
      const suffix = String(variables?.raw_suffix ?? '').trim();
      const display = raw.replaceAll('_', ' ').toUpperCase();
      return suffix ? `${display} ${suffix}` : display;
    ]]]
  styles:
    card:
    - min-height: >
        [[[ return variables?.mobile_compact ? '64px' : 'auto'; ]]]
    - padding: >
        [[[ return variables?.mobile_compact ? '4px 5px' : '10px 12px'; ]]]
    - border-color: >
        [[[
          const severity = String(variables?.status_severity ?? 'critical');
          const mobile = Boolean(variables?.mobile_compact);
          if (severity === 'active') return 'rgba(30,136,229,0.45)';
          if (severity === 'warning') return 'rgba(245,124,0,0.45)';
          if (severity === 'healthy') return 'rgba(67,160,71,0.45)';
          return mobile ? 'rgba(183,28,28,1)' : 'rgba(229,57,53,0.40)';
        ]]]
    - background: >
        [[[
          const severity = String(variables?.status_severity ?? 'critical');
          const mobile = Boolean(variables?.mobile_compact);
          if (severity === 'active') return 'rgba(227,242,253,0.90)';
          if (severity === 'warning') return 'rgba(255,243,224,0.85)';
          if (severity === 'healthy') return 'rgba(232,245,233,0.85)';
          return mobile ? 'rgba(183,28,28,0.96)' : 'rgba(255,235,238,0.85)';
        ]]]
    grid:
    - grid-template-areas: >
        [[[ return variables?.mobile_compact ? '"i n" "s s"' : '"i n" "i s"'; ]]]
    - grid-template-columns: >
        [[[ return variables?.mobile_compact ? '18px minmax(0, auto)' : '28px 1fr'; ]]]
    - grid-template-rows: >
        [[[ return variables?.mobile_compact ? '18px 12px' : 'min-content min-content'; ]]]
    - column-gap: >
        [[[ return variables?.mobile_compact ? '5px' : '0'; ]]]
    - row-gap: >
        [[[ return variables?.mobile_compact ? '3px' : '0'; ]]]
    - justify-content: >
        [[[ return variables?.mobile_compact ? 'center' : 'stretch'; ]]]
    icon:
    - display: >
        [[[ return 'block'; ]]]
    - width: >
        [[[ return '18px'; ]]]
    - justify-self: >
        [[[ return variables?.mobile_compact ? 'end' : 'start'; ]]]
    - color: >
        [[[
          const severity = String(variables?.status_severity ?? 'critical');
          const mobile = Boolean(variables?.mobile_compact);
          if (severity === 'active') return 'rgba(25,118,210,1)';
          if (severity === 'warning') return 'rgba(245,124,0,1)';
          if (severity === 'healthy') return 'rgba(46,125,50,1)';
          return mobile ? '#ffffff' : 'rgba(198,40,40,1)';
        ]]]
    name:
    - color: >
        [[[
          if (!variables?.mobile_compact) return 'var(--primary-text-color)';
          const severity = String(variables?.status_severity ?? 'critical');
          if (severity === 'active') return '#0d47a1';
          if (severity === 'warning') return '#e65100';
          return severity === 'healthy' ? '#1b5e20' : '#ffffff';
        ]]]
    - font-size: >
        [[[ return variables?.mobile_compact ? '12px' : '13px'; ]]]
    - font-weight: 900
    - line-height: >
        [[[ return variables?.mobile_compact ? '12px' : 'normal'; ]]]
    - justify-self: >
        [[[ return 'start'; ]]]
    - white-space: >
        [[[ return variables?.mobile_compact ? 'normal' : 'nowrap'; ]]]
    - overflow: >
        [[[ return variables?.mobile_compact ? 'visible' : 'hidden'; ]]]
    - text-overflow: >
        [[[ return variables?.mobile_compact ? 'clip' : 'ellipsis'; ]]]
    - text-align: >
        [[[ return 'left'; ]]]
    - opacity: 1
    state:
    - color: >
        [[[
          const severity = String(variables?.status_severity ?? 'critical');
          const mobile = Boolean(variables?.mobile_compact);
          if (severity === 'active') return 'rgba(25,118,210,1)';
          if (severity === 'warning') return 'rgba(230,81,0,1)';
          if (severity === 'healthy') return 'rgba(46,125,50,1)';
          return mobile ? '#ffffff' : 'rgba(198,40,40,1)';
        ]]]
    - font-size: >
        [[[ return variables?.mobile_compact ? '11px' : '12px'; ]]]
    - font-weight: 900
    - line-height: >
        [[[ return variables?.mobile_compact ? '12px' : 'normal'; ]]]
    - justify-self: >
        [[[ return variables?.mobile_compact ? 'center' : 'start'; ]]]
    - opacity: 1

bearstone_mobile_header:
  template: bearstone_infra_panel_header_icon
  styles:
    card:
    - padding: 10px 2px 8px 2px
    name:
    - font-size: 22px
    - line-height: 26px

bearstone_nav_back:
  template: bearstone_infra_list_row
  show_state: false
  styles:
    card:
    - min-height: 60px
    - padding: 14px 16px
    grid:
    - grid-template-areas: '"i n"'
    - grid-template-columns: 32px 1fr
    icon:
    - width: 26px
    name:
    - font-size: 17px
    - line-height: 22px

bearstone_mobile_control:
  template: bearstone_overview_ribbon_control
  variables:
    mobile_compact: true
  styles:
    card:
    - min-height: 112px
    - padding: 12px 10px
    - border-radius: 20px
    grid:
    - grid-template-areas: '"i n" "s s"'
    - grid-template-columns: 28px minmax(0, auto)
    - grid-template-rows: 32px min-content
    - column-gap: 8px
    - row-gap: 6px
    - justify-content: center
    icon:
    - width: 28px
    - justify-self: end
    name:
    - font-size: 18px
    - line-height: 22px
    - justify-self: start
    - white-space: normal
    - overflow: visible
    - text-overflow: clip
    - text-align: center
    state:
    - font-size: 15px
    - line-height: 18px
    - justify-self: center
    - text-align: center

bearstone_overview_sleep_summary:
  template: bearstone_infra_kpi
  show_label: true
  show_icon: true
  variables:
    score_attribute: ''
    green_threshold: 88
    amber_threshold: 65
  styles:
    card:
    - min-height: 62px
    - padding: 8px 12px
    grid:
    - grid-template-areas: '"i n" "s s" "l l"'
    - grid-template-columns: 18px minmax(0, auto)
    - grid-template-rows: 20px min-content 14px
    - column-gap: 6px
    - row-gap: 1px
    - justify-content: center
    icon:
    - width: 18px
    - color: var(--primary-color)
    - justify-self: end
    name:
    - font-size: 12px
    - font-weight: 900
    - opacity: 0.75
    state:
    - font-size: 26px
    - font-weight: 900
    - opacity: 1
    - color: >
        [[[
          const attribute = String(variables?.score_attribute ?? '').trim();
          const score = Number(attribute ? entity?.attributes?.[attribute] : entity?.state);
          const greenThreshold = Number(variables?.green_threshold ?? 88);
          const amberThreshold = Number(variables?.amber_threshold ?? 65);
          if (!Number.isFinite(score)) return 'var(--secondary-text-color)';
          if (score >= greenThreshold) return '#2e7d32';
          if (score >= amberThreshold) return '#f9a825';
          return '#c62828';
        ]]]
    label:
    - font-size: 11px
    - font-weight: 700
    - line-height: 14px

bearstone_health_kpi:
  template: bearstone_infra_kpi
  variables:
    detail_layout: false
  styles:
    grid:
    - grid-template-areas: >
        [[[
          return variables?.detail_layout
            ? '"i n" "s s" "l l"'
            : '"i n" "s s"';
        ]]]
    - grid-template-columns: 18px minmax(0, auto)
    - grid-template-rows: >
        [[[
          return variables?.detail_layout
            ? '20px min-content minmax(18px, auto)'
            : '20px min-content';
        ]]]
    - column-gap: 6px
    - justify-content: center
    name:
    - font-size: 12px
    - font-weight: 900
    - opacity: 0.8
    - justify-self: start
    - width: auto
    - min-width: 0
    - white-space: normal
    - overflow: visible
    - text-overflow: clip
    - line-height: 15px
    state:
    - font-size: 17px
    - font-weight: 900
    - opacity: 1
    - justify-self: stretch
    - width: 100%
    - min-width: 0
    - white-space: normal
    - overflow: visible
    - text-overflow: clip
    - line-height: 22px
    - text-align: center
    label:
    - font-size: 12px
    - font-weight: 600
    - opacity: 0.78
    - justify-self: stretch
    - width: 100%
    - min-width: 0
    - white-space: normal
    - overflow: visible
    - text-overflow: clip
    - text-align: center
    - line-height: 16px

bearstone_infra_update_row:
  template: bearstone_infra_list_row
  icon: mdi:package-up
  show_label: true
  variables:
    apt_severity: >
      [[[
        const state = String(entity?.state ?? '').toUpperCase();
        if (!state || state.includes('UNKNOWN') || state.includes('UNAVAILABLE') || state.includes('ERROR')) return 'error';
        const host = entity?.entity_id?.match(/^sensor\.(docker_\d+)_apt_status$/)?.[1];
        const dueRaw = host ? states[`input_datetime.apt_${host}_action_due`]?.state : null;
        const due = dueRaw && !['unknown', 'unavailable', 'none'].includes(String(dueRaw).toLowerCase())
          ? new Date(String(dueRaw).replace(' ', 'T'))
          : null;
        const actionable = state.includes('PENDING') || state.includes('REBOOT REQ');
        if (actionable && due instanceof Date && !Number.isNaN(due.getTime()) && Date.now() > due.getTime()) return 'overdue';
        if (actionable) return 'scheduled';
        if (state.includes('NO UPDATES') || state.includes('UPDATED')) return 'healthy';
        return 'neutral';
      ]]]
  state_display: >
    [[[ return String(entity?.state ?? 'NO DATA').toUpperCase(); ]]]
  label: >
    [[[
      const state = String(entity?.state ?? '').toUpperCase();
      const host = entity?.entity_id?.match(/^sensor\.(docker_\d+)_apt_status$/)?.[1];
      const dueRaw = host ? states[`input_datetime.apt_${host}_action_due`]?.state : null;
      const parseDate = (raw) => {
        if (!raw || ['unknown', 'unavailable', 'none', ''].includes(String(raw).toLowerCase())) return null;
        const value = new Date(String(raw).replace(' ', 'T'));
        return Number.isNaN(value.getTime()) ? null : value;
      };
      const formatTime = (value) => value.toLocaleTimeString('en-US', {
        hour: 'numeric',
        minute: '2-digit'
      }).toUpperCase();
      const formatDue = (value) => {
        const weekday = value.toLocaleDateString('en-US', { weekday: 'short' }).toUpperCase();
        return `${weekday} ${formatTime(value)}`;
      };
      const actionable = state.includes('PENDING') || state.includes('REBOOT REQ');
      const due = parseDate(dueRaw);
      if (actionable) {
        const action = state.includes('REBOOT REQ') ? 'REBOOT' : 'UPDATE';
        return due ? `${action} ${formatDue(due)}` : `${action} SCHEDULED`;
      }
      return 'NO ACTION SCHEDULED';
    ]]]
  styles:
    grid:
    - grid-template-areas: '"i n s" "i l l"'
    - grid-template-columns: 24px 1fr auto
    - grid-template-rows: min-content min-content
    card:
    - border-color: >
        [[[
          if (variables.apt_severity === 'overdue') return 'rgba(229,57,53,0.40)';
          if (variables.apt_severity === 'error' || variables.apt_severity === 'scheduled') return 'rgba(245,124,0,0.45)';
          if (variables.apt_severity === 'healthy') return 'rgba(67,160,71,0.45)';
          return 'var(--divider-color)';
        ]]]
    - background: >
        [[[
          if (variables.apt_severity === 'overdue') return 'rgba(255,235,238,0.85)';
          if (variables.apt_severity === 'error' || variables.apt_severity === 'scheduled') return 'rgba(255,243,224,0.85)';
          if (variables.apt_severity === 'healthy') return 'rgba(232,245,233,0.85)';
          return 'rgba(0,0,0,0.02)';
        ]]]
    icon:
    - color: >
        [[[
          if (variables.apt_severity === 'overdue') return 'rgba(198,40,40,1)';
          if (variables.apt_severity === 'error' || variables.apt_severity === 'scheduled') return 'rgba(230,81,0,1)';
          if (variables.apt_severity === 'healthy') return 'rgba(46,125,50,1)';
          return 'var(--primary-color)';
        ]]]
    state:
    - color: >
        [[[
          if (variables.apt_severity === 'overdue') return 'rgba(198,40,40,1)';
          if (variables.apt_severity === 'error' || variables.apt_severity === 'scheduled') return 'rgba(230,81,0,1)';
          if (variables.apt_severity === 'healthy') return 'rgba(46,125,50,1)';
          return 'var(--secondary-text-color)';
        ]]]
    label:
    - grid-area: l
    - justify-self: start
    - padding-top: 3px
    - font-size: 11px
    - font-weight: 700
    - letter-spacing: 0.02em
    - opacity: 0.72
    - white-space: normal
    - text-align: left
    - color: >
        [[[
          if (variables.apt_severity === 'overdue') return 'rgba(198,40,40,1)';
          if (variables.apt_severity === 'error' || variables.apt_severity === 'scheduled') return 'rgba(230,81,0,1)';
          if (variables.apt_severity === 'healthy') return 'rgba(46,125,50,1)';
          return 'var(--secondary-text-color)';
        ]]]

bearstone_infra_update_tile:
  template: bearstone_infra_update_row
  styles:
    card:
    - min-height: 96px
    - padding: 10px
    grid:
    - grid-template-areas: '"i n" "s s" "l l"'
    - grid-template-columns: 18px minmax(0, auto)
    - grid-template-rows: 20px min-content min-content
    - column-gap: 6px
    - row-gap: 3px
    - justify-content: center
    icon:
    - width: 18px
    - justify-self: end
    name:
    - justify-self: start
    - text-align: left
    - white-space: normal
    state:
    - justify-self: start
    - padding-left: 0
    - text-align: center
    - white-space: normal
    - line-height: 16px
    label:
    - justify-self: center
    - padding-top: 0
    - text-align: left
    - line-height: 14px
